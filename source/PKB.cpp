#pragma once

#include <stdio.h>
#include <iostream>
#include <string>
#include <vector>

using namespace std;

#include "pkb.h"
#include "t_node.h"

int PKB::SetProcToAst(PROC p, TNode* r) { return NULL; }

TNode* PKB::GetRootAst(PROC p) { return NULL; }
